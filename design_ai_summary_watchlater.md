# 稍后再看 + AI总结 功能设计方案

## 一、需求概述

### 1.1 核心目标
在"稍后再看"列表页面，为用户提供视频的AI总结信息，帮助用户在正式观看前快速判断是否想观看该视频，同时提供便捷的"跳过/移除"操作。

### 1.2 最终决策

| 决策项 | 选择 | 说明 |
|--------|------|------|
| **功能开关** | 设置项控制 | 可在设置中开启/关闭AI总结视图Tab |
| **加载策略** | 预加载方式 | 列表滚动时预加载可见区域的AI总结 |
| **章节跳转** | 已实现 | 现有AI总结组件已支持章节点击跳转 |
| **移除策略** | 选择确认方式 | 模仿现有稍后再看的多选确认方式 |
| **UI方案** | 方案三：独立总结视图 | 新增「AI总结」Tab页面 |
| **开发方法** | TDD（测试驱动开发） | 先写测试，再实现功能 |

---

## 二、现有实现分析

### 2.1 稍后再看页面结构

**当前实现**：
- 页面入口：`/workspace/lib/pages/later/view.dart`
- 控制器逻辑：`/workspace/lib/pages/later/controller.dart`
- 列表组件：`/workspace/lib/pages/video/medialist/view.dart`

**当前列表项布局**（固定高度100px）：
```
┌─────────────────────────────────────────────────┐
│ ┌──────┐  标题（最多2行）                        │ ← 封面 140.8x88
│ │      │  UP主名称                               │ ← 间距10px
│ │ 封面 │  播放数 · 弹幕数                        │ ← 底部对齐
│ │      │                                        │
│ └──────┘                                        │   [删除按钮-条件显示]
└─────────────────────────────────────────────────┘
```

### 2.2 AI总结数据来源

**API调用**：`/workspace/lib/http/video.dart#L783`
```dart
VideoHttp.aiConclusion({
  required String bvid,
  required int cid,
  int? upMid,
})
```

**返回数据模型**：`/workspace/lib/models_new/video/video_ai_conclusion/model_result.dart`
```dart
class AiConclusionResult {
  String? summary;           // AI生成的文字总结
  List<Outline>? outline;    // 视频大纲（带时间戳）
}

class Outline {
  String? title;              // 章节标题
  List<PartOutline>? partOutline;  // 章节内的时间点
}

class PartOutline {
  int? timestamp;            // 时间戳（秒）
  String? content;            // 时间点内容描述
}
```

---

## 三、UI设计方案

### 3.1 方案一：列表项扩展（推荐）

**设计思路**：在不改变整体布局的情况下，通过展开/折叠方式展示AI总结

**展开状态布局**（高度从100px扩展到200px左右）：
```
┌─────────────────────────────────────────────────┐
│ ┌──────┐  标题（最多2行）                        │
│ │      │  UP主名称 · 播放数 · 弹幕数               │
│ │ 封面 │  ──────────────────────────             │
│ │      │  🤖 AI总结                              │ ← 展开区域
│ │ 140x │  这是一段AI生成的视频总结，描述了视频...  │
│ │  88  │                                         │
│ └──────┘  [查看更多] [不感兴趣-移除]              │
│           ──────────────────────────             │
│                             [播放按钮] [删除✕]    │
└─────────────────────────────────────────────────┘
```

**交互逻辑**：
- 点击封面/标题 → 正常播放视频
- 点击AI总结区域 → 展开/折叠总结内容
- 展开后显示：
  - 总结文本（限制3-4行，支持展开查看全部）
  - 章节列表（可选，最多显示3个章节，点击可跳转）
  - 「查看完整总结」→「不感兴趣，移除」按钮

**优点**：
- 保持原有操作习惯
- 信息渐进式展示，不造成视觉负担
- 展开态占用空间合理

**缺点**：
- 单次只能查看一个视频的总结
- 展开操作增加一步交互

---

### 3.2 方案二：悬浮预览卡片

**设计思路**：悬停/长按显示AI总结浮层

**布局示意**：
```
正常列表显示 ↓ 长按/悬停
              ↓
┌─────────────────────────────────────────────────┐
│ ┌──────┐  标题...                    [×]        │
│ │ 封面 │  ────────────────────────────         │
│ └──────┘  ┌─────────────────────────────┐      │
│           │ 🤖 AI总结                     │      │ ← 浮层卡片
│           │ 这是一段视频内容总结...        │      │   最大高度180px
│           │ ─────────────────────────── │      │   圆角12px
│           │ 第一章 00:30 xxxxxx         │      │   阴影
│           │ 第二章 02:15 xxxxxx         │      │
│           │ [查看完整] [移除] [播放]      │      │
│           └─────────────────────────────┘      │
└─────────────────────────────────────────────────┘
```

**交互逻辑**：
- 长按列表项（手机）→ 显示AI总结浮层
- 浮层自动出现在列表项上方或下方（根据屏幕位置）
- 浮层内提供：播放、移除、关闭按钮
- 点击浮层外部或播放/移除后关闭浮层

**优点**：
- 不改变列表布局
- 可以快速浏览多个视频的总结
- 浮层可以拖动定位

**缺点**：
- 长按操作对部分用户不够直观
- 浮层可能被其他UI元素遮挡

---

### 3.3 方案三：独立总结视图

**设计思路**：新增一个「AI总结」的Tab页面

**页面布局**：
```
┌─────────────────────────────────────────────────┐
│  稍后再看  │  AI总结(12) │                       │ ← 新增Tab
├─────────────────────────────────────────────────┤
│ ┌──────┐  ┌─────────────────────────────────┐  │
│ │ 封面 │  │ 标题：xxx                        │  │
│ │ 140x │  │ UP主：xxx · 播放：xxx            │  │
│ │  88  │  │                                 │  │
│ └──────┘  │ 🤖 总结：                         │  │
│           │ 这是一段视频内容总结，描述了...    │  │
│           │ [展开]                           │  │
│           └─────────────────────────────────┘  │
│                                          [播放] │
├─────────────────────────────────────────────────┤
│ ┌──────┐  ... (下一个视频卡片)                   │
└─────────────────────────────────────────────────┘
```

**交互逻辑**：
- 切换Tab后，展示所有有AI总结的视频
- 每个卡片显示：封面 + 标题 + AI总结摘要
- 点击卡片展开完整总结
- 提供播放和移除按钮

**优点**：
- 信息密度高，一屏可看多个总结
- 与原有功能完全解耦
- 用户可选择只浏览有AI总结的视频

**缺点**：
- 需要用户主动切换Tab
- 增加页面复杂度

---

## 四、方案对比

| 维度 | 方案一：列表扩展 | 方案二：悬浮卡片 | 方案三：独立视图 |
|------|----------------|----------------|----------------|
| **实现复杂度** | 中等 | 较高 | 较低 |
| **用户认知成本** | 低 | 中等 | 中等 |
| **信息密度** | 低 | 中 | 高 |
| **交互步骤** | 1步展开 | 1步长按 | 0步切换 |
| **适合场景** | 偶尔查看 | 快速浏览多个 | 专门筛选 |
| **屏幕适配** | 好 | 需处理边缘 | 好 |
| **批量操作** | 不支持 | 不支持 | 可支持 |

---

## 五、推荐方案：方案一（渐进式展开）+ 快捷操作

### 5.1 核心设计原则

1. **渐进式披露**：默认显示基本信息，AI总结默认折叠
2. **最小化操作成本**：移除操作一步完成，无需确认弹窗
3. **视觉层次分明**：信息分块清晰，操作按钮易于发现
4. **尊重用户习惯**：不改变原有的播放入口

### 5.2 详细布局规格

**折叠态**（默认，高度100px）：
```
┌─────────────────────────────────────────────────┐
│┌────────┐  标题（最多2行，超出省略）               │
││        │  UP主名称 · 播放数 · 弹幕数 · 时长      │
││ 封面   │                                        │
││ 140x88 │                           [🤖 AI] [▶] │
││        │                                        │
│└────────┘                                        │
└─────────────────────────────────────────────────┘
```

**展开态**（高度约180-220px）：
```
┌─────────────────────────────────────────────────┐
│┌────────┐  标题（最多2行，超出省略）               │
││        │  UP主名称 · 播放数 · 弹幕数 · 时长      │
││ 封面   │  ────────────────────────────          │
││ 140x88 │  🤖 AI总结                             │
││        │  这是一段视频内容总结，描述了视频的主... │
││        │  ┌─────────────────────────────┐       │
││        │  │ 第一章 00:30 xxxxxx        │       │ ← 章节预览
││        │  │ 第二章 02:15 xxxxxx        │       │   最多显示3个
││        │  └─────────────────────────────┘       │
│└────────┘  [展开全文]    [不感兴趣] [播放]       │
└─────────────────────────────────────────────────┘
```

### 5.3 交互细节

**AI总结按钮位置**：
- 位于封面右侧，与播放按钮平齐
- 使用图标+文字组合：`🤖 AI`
- 尺寸：44x32（符合触摸目标最小44px要求）

**展开动画**：
- 使用 `AnimatedContainer` 或 `AnimatedSize`
- 动画时长：250ms
- 缓动曲线：`Curves.easeOut`

**章节列表**：
- 每行高度：32px
- 显示格式：`⏱ 00:30 章节标题`
- 最多显示3个，超出显示「共X个章节」
- 点击章节 → 记录跳转时间点，播放时自动跳转

**移除按钮**：
- 样式：`OutlinedButton` 或 `TextButton`
- 颜色：`error` 主题色
- 点击后直接移除，无需确认
- 提供「撤销」Toast提示（3秒内可撤销）

---

## 六、展开全文视图

当用户点击「展开全文」时，显示完整AI总结：

### 6.1 全屏展开（推荐）

```
┌─────────────────────────────────────────────────┐
│ ← 返回                          [不感兴趣] [播放] │
├─────────────────────────────────────────────────┤
│┌────────┐                                        │
││        │  视频标题                                │
││ 封面   │  UP主：xxx · 播放：xxx · 时长：xx:xx   │
││ 140x88 │                                        │
│└────────┘                                        │
├─────────────────────────────────────────────────┤
│  🤖 AI视频总结                                   │
│  ────────────────────────────────────────        │
│                                                     │
│  这是完整的AI生成视频总结内容，可以包含多段文字，    │
│  详细描述了视频的主要内容、观点和结论...           │
│                                                     │
│  ────────────────────────────────────────        │
│                                                     │
│  📑 视频章节                                       │
│  ┌─────────────────────────────────────────┐     │
│  │ 第一章 00:00 开场介绍                      │     │
│  │ 第二章 00:30 主要内容                      │     │
│  │ 第三章 02:15 深入讲解                      │     │
│  │ 第四章 05:00 总结回顾                      │     │
│  │ 第五章 08:30 结尾彩蛋                      │     │
│  └─────────────────────────────────────────┘     │
│  [查看完整章节列表]                               │
│                                                     │
├─────────────────────────────────────────────────┤
│                        [开始播放] 或 [从当前章节]  │
└─────────────────────────────────────────────────┘
```

### 6.2 交互功能

**开始播放**：
- 点击后直接进入视频播放
- 如果选择「从当前章节」，播放时自动跳转到该时间点

**不感兴趣（移除）**：
- 点击后直接移除出"稍后再看"列表
- 显示撤销提示：「已移除 · 撤销」(3秒)
- 撤销后恢复原位

---

## 七、屏幕适配策略

### 7.1 小屏幕（宽度 < 360px）

- 封面宽度调整为 120px
- 标题单行显示，超出省略
- 隐藏播放数/弹幕数，仅保留封面角标

### 7.2 大屏幕/平板（宽度 > 600px）

- 封面宽度保持 160px
- 可以考虑左右两列布局
- 每个视频卡片信息更完整

### 7.3 横屏模式

- 保持原有横屏列表布局
- AI总结以侧边栏形式展示
- 不影响视频播放区域

---

## 八、技术实现要点

### 8.1 数据预加载策略

为避免卡顿，建议采用以下策略：

```dart
// 1. 首次加载列表时，不加载AI总结
// 2. 用户展开某个视频的总结时，按需加载
Future<void> loadAiSummary(String bvid, int cid) async {
  // 检查缓存
  if (cache.contains(bvid)) {
    return cache.get(bvid);
  }
  
  // 加载新数据
  final result = await UgcIntroController.getAiConclusion(bvid, cid, null);
  cache.put(bvid, result);
  return result;
}
```

### 8.2 缓存策略

- 使用内存缓存：保存已加载的AI总结
- 缓存大小限制：最多缓存20条
- 缓存失效时间：24小时

### 8.3 性能优化

- 列表项高度固定（避免动态计算）
- 使用 `ListView.builder` 优化长列表
- AI总结加载使用 `FutureBuilder` + 占位符

---

## 九、待讨论问题

### 9.1 功能开关
- 是否需要设置项控制「是否显示AI总结按钮」？
- 建议：默认开启，可关闭

### 9.2 AI总结加载时机
- **方案A**：点击时加载（节省流量，但有等待）
- **方案B**：列表滚动时预加载可见区域（体验好，但增加请求）
- **建议**：方案A + 预加载下一条（折中）

### 9.3 移除后撤销策略
- **方案A**：仅Toast撤销（简单，但只支持单次）
- **方案B**：撤销列表支持多条（复杂，但体验好）
- **建议**：方案A

### 9.4 章节跳转
- 是否支持点击章节后记录时间点？
- 播放时自动跳转到选中的时间点
- 建议：支持

---

## 十、下一步行动

1. **确认方案**：请选择上述方案中的一个（或组合）
2. **优先级排序**：核心功能 → 增强功能 → 优化功能
3. **技术评审**：评估API限制、数据结构扩展
4. **UI评审**：确认视觉效果、交互细节

---

*文档版本：v1.0*
*最后更新：2026-05-06*
